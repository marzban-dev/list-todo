"use client";

import CompFolderOpen from "@/components/svg-components/svgs/folder-open.svg"

const IconFolderOpen = (props : {className?:string; style?:React.CSSProperties}) => {
    return <CompFolderOpen {...props}/>
}

export default IconFolderOpen